# GitHub 公开发布检查清单

## 必须执行

- 只发布虚构的 3 个示例班，不提交任何真实学生姓名、学号或班级表格。
- 不提交 `班级名单.json`、`点名历史.json`、`settings.json` 及其备份。
- 不提交 `人员名单/` 目录。
- 不提交授权状态不明确的 `music/好运来.mp3`。
- 保留 3 首 CC0 音乐及《音乐授权说明.md》。
- 确认根目录包含 MIT `LICENSE`，README 已区分代码与音乐素材的授权范围。

## Git 历史

当前私人仓库的旧提交中曾出现真实名单。即使当前文件已经改成示例数据，直接公开旧 `.git` 历史仍可能泄露信息。

正式发布时应：

1. 把清理后的公开文件复制到一个全新的空目录；
2. 不复制当前项目的 `.git`、本地 JSON 数据和 `人员名单/`；
3. 在新目录中执行 `git init`；
4. 再进行首次提交并上传 GitHub；
5. 上传前检查暂存文件列表，确认没有真实名单和私人音乐。

## 发布前验证

- 在没有任何本地 JSON 文件的干净目录中启动程序；
- 确认只显示“示例一班、示例二班、示例三班”；
- 确认姓名均为“甲同学01”等虚构标识；
- 确认 EXE 中只内嵌 3 首 CC0 音乐；
- 确认窗口标题、托盘提示和说明文档统一使用“点名啦”。
